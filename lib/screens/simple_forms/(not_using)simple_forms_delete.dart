import 'package:flutter/material.dart';

class SimpleFormsDelete extends StatelessWidget {
  const SimpleFormsDelete({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
